import { useBooleanState } from '@/hooks/useBooleanState';
import { List } from 'postcss/lib/list';

interface MenuProps {
  name: string,
  href: string,
  list?: MenuProps[]
}

export function DropMenu(MenuList: any) {
  console.log(MenuList);
  return (
    <>

    </>
  )
}
